import { smallTeam } from "./small-teams";
import { smallUser, user } from "./users";

export const squad = {
  name: 'My Squad',
  bio: 'My bio',
  teams: [smallTeam],
  captain: user,
  members: [smallUser]
}